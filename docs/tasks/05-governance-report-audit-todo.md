# 举报、治理与审计任务清单

本文档用于收口举报提交流程、后台处理流转、治理动作联动和审计增强。

## 1. 任务来源

- `docs/需求文档/后台治理模块-PRD.md`
- `docs/需求文档/举报处理流转草案.md`
- `docs/需求文档/项目需求草案-PRD.md`
- `docs/需求文档/项目执行任务清单.md`
- `docs/需求文档/数据库表优化方案.md`

## 2. 当前状态

**当前阶段：测试回补完成。举报提交流程、后台管理、状态流转、处理日志和治理动作联动（删除内容/撤回消息/禁言/封禁）已落地；服务级测试已回补。禁言当前限定为会话级，全站禁言待后续扩展。**

```
已具备:
  ✅ 文章 / 评论 / 聊天消息基础管理接口
  ✅ 聊天消息撤回、成员禁言等治理动作
  ✅ 后台日志基础能力
  ✅ sys_report_record 表结构预留
  ✅ sys_report_handle_log 表结构预留
  ✅ 举报对象类型枚举（article/comment/chat_message）
  ✅ 用户侧举报提交、查询我的举报
  ✅ 后台举报分页筛选、详情、接手、处理、驳回
  ✅ 举报状态流转（pending→processing→handled/rejected）
  ✅ 处理日志留档
  ✅ 重复举报频率限制（24小时内同用户同对象一次）
  ✅ 处理完成写入审计日志（SysAuditLogService）
  ✅ 治理动作联动：删除文章/评论、撤回聊天消息
  ✅ 聊天消息举报→会话级禁言（默认24小时）
  ✅ 封禁账号联动：banUserByReport（超管权限保护，审计留档）
  ✅ 服务级测试回补完成

待推进:
  ⏳ 全站禁言能力（当前仅支持会话级禁言）
```text

## 3. T11 举报提交流程

### 3.1 目标

- 用户可举报文章、评论和聊天消息。
- 系统生成统一举报记录。
- 后台可分页处理举报。

### 3.2 数据与模型

- [x] 复核 `sys_report_record` 字段是否覆盖对象类型、对象 ID、举报人、原因、补充说明、状态、处理人、处理时间。
- [x] 复核 `sys_report_handle_log` 字段是否覆盖状态流转、处理结果、处罚类型、备注、操作人。
- [x] 定义举报对象类型：`article`、`comment`、`chat_message`。
- [x] 定义举报状态：`pending`、`processing`、`processed`、`rejected`。
- [x] 定义处理结果：`delete_content`、`revoke_message`、`mute_user`、`ban_user`、`record_only`。
- [x] 定义处罚类型：`content_delete`、`message_revoke`、`temporary_mute`、`account_ban`、`none`。

### 3.3 用户侧接口

- [x] `POST /api/user/reports`：提交举报。
- [x] `GET /api/user/reports`：查询我的举报记录。
- [x] `GET /api/user/reports/{id}`：查询我的举报详情。
- [x] 提交举报时校验对象存在且当前用户可见。
- [x] 限制同一用户对同一对象重复举报频率。

### 3.4 后台接口

- [x] `GET /api/sys/reports`：分页筛选举报。
- [x] `GET /api/sys/reports/{id}`：举报详情。
- [x] `PUT /api/sys/reports/{id}/take`：接手举报，转为处理中。
- [x] `PUT /api/sys/reports/{id}/handle`：处理举报。
- [x] `PUT /api/sys/reports/{id}/reject`：驳回举报。
- [x] `GET /api/sys/reports/{id}/logs`：处理日志。

### 3.5 状态流转

- [x] `pending -> processing`：管理员接手。
- [x] `pending -> processed`：管理员直接处理。
- [x] `processing -> processed`：处理完成。
- [x] `pending -> rejected`：直接驳回。
- [x] `processing -> rejected`：核查后驳回。
- [x] 终态举报普通管理员不可再次修改。
- [x] 超级管理员可接管并终结异常举报。（overrideClaim 已实现）

### 3.6 测试

- [x] 用户举报文章成功。
- [x] 用户举报评论成功。
- [x] 用户举报聊天消息成功。
- [x] 举报不存在对象被拦截。
- [x] 重复举报频率限制生效。
- [x] 管理员接手后状态变为处理中。
- [x] 终态举报重复处理被拦截。

## 4. 治理动作联动

### 4.1 删除内容

- [x] 举报文章成立时可删除或下架文章。
- [x] 举报评论成立时可删除或隐藏评论。
- [x] 删除内容动作必须调用对应业务服务，不直接改表绕过一致性逻辑。
- [x] 删除动作写入举报处理日志和系统审计日志。

### 4.2 撤回消息

- [x] 举报聊天消息成立时可调用聊天后台撤回能力。
- [x] 已撤回消息重复撤回保持幂等或给出明确业务错误。
- [x] 撤回后需要推送消息更新事件。

### 4.3 禁言用户

- [ ] 支持按场景禁言：大厅、主题频道、群、全站。（当前已实现聊天消息举报→会话级禁言）
- [x] 记录禁言时长、原因、操作人。
- [x] 禁言动作与聊天发送校验联动。
- [x] 临时禁言到期后自动恢复或发送时动态判断。

### 4.4 封禁账号

- [x] 支持账号禁用。（banUserByReport：超管权限保护，跳过MFA，审计留档）
- [x] 禁用后用户无法登录或无法继续使用互动能力。（invalidateUserSessions）
- [x] 封禁动作必须有高风险审计。
- [x] 超级管理员可解封。

### 4.5 仅记录不处罚

- [x] 支持举报成立但不处罚。
- [x] 支持举报不成立并驳回。
- [x] 两种情况都需要明确处理说明。

## 5. 审计增强

- [x] 举报处理动作写入 `sys_report_handle_log`。
- [x] 高风险动作同步写入系统操作日志。
- [x] 审计记录包含处理前状态和处理后状态。
- [x] 普通管理员不能删除审计记录。（审计控制器无删除端点）
- [x] 超级管理员接管行为必须单独记录。（overrideClaim 写入 OVERRIDE_CLAIM_REPORT 审计类型）

## 6. 通知预留

- [x] 举报提交后后台可见。
- [ ] 举报处理结果是否通知举报人先预留字段。
- [ ] 被举报人是否通知先不强制实现。
- [ ] 后续如接入通知中心，应遵守用户通知设置。

## 7. 非目标

- [x] 第一阶段不做申诉流程。
- [x] 第一阶段不做自动审核。
- [x] 第一阶段不做复杂风控画像。
- [x] 第一阶段不做批量处理，除非后台需求进一步明确。

## 8. 完成标志

- 用户可举报文章、评论、聊天消息。
- 管理员可接手、处理、驳回举报。
- 处理动作能联动内容、聊天和账号治理。
- 举报状态、处理结果和处罚类型完整留档。
- 超级管理员接管和高风险动作具备审计记录。
- 对应 API 文档、需求文档和任务清单已同步更新。
