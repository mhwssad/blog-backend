# 举报、治理与审计任务清单

本文档用于收口举报提交流程、后台处理流转、治理动作联动和审计增强。

## 1. 任务来源

- `docs/需求文档/后台治理模块-PRD.md`
- `docs/需求文档/举报处理流转草案.md`
- `docs/需求文档/项目需求草案-PRD.md`
- `docs/需求文档/项目执行任务清单.md`
- `docs/需求文档/数据库表优化方案.md`

## 2. 当前状态

**当前阶段：待开始。内容、聊天和后台管理已有基础治理动作，统一举报入口和处理流转尚未落地。**

```
已具备:
  ✅ 文章 / 评论 / 聊天消息基础管理接口
  ✅ 聊天消息撤回、成员禁言等治理动作
  ✅ 后台日志基础能力
  ✅ sys_report_record 表结构预留
  ✅ sys_report_handle_log 表结构预留

待推进:
  ⏳ 举报文章 / 评论 / 聊天消息统一入口
  ⏳ 举报状态流转
  ⏳ 处理结果和处罚类型留档
  ⏳ 治理动作与业务模块联动
  ⏳ 管理员处理权限和超级管理员接管
```

## 3. T11 举报提交流程

### 3.1 目标

- 用户可举报文章、评论和聊天消息。
- 系统生成统一举报记录。
- 后台可分页处理举报。

### 3.2 数据与模型

- [ ] 复核 `sys_report_record` 字段是否覆盖对象类型、对象 ID、举报人、原因、补充说明、状态、处理人、处理时间。
- [ ] 复核 `sys_report_handle_log` 字段是否覆盖状态流转、处理结果、处罚类型、备注、操作人。
- [ ] 定义举报对象类型：`article`、`comment`、`chat_message`。
- [ ] 定义举报状态：`pending`、`processing`、`processed`、`rejected`。
- [ ] 定义处理结果：`delete_content`、`revoke_message`、`mute_user`、`ban_user`、`record_only`。
- [ ] 定义处罚类型：`content_delete`、`message_revoke`、`temporary_mute`、`account_ban`、`none`。

### 3.3 用户侧接口

- [ ] `POST /api/user/reports`：提交举报。
- [ ] `GET /api/user/reports`：查询我的举报记录。
- [ ] `GET /api/user/reports/{id}`：查询我的举报详情。
- [ ] 提交举报时校验对象存在且当前用户可见。
- [ ] 限制同一用户对同一对象重复举报频率。

### 3.4 后台接口

- [ ] `GET /api/sys/reports`：分页筛选举报。
- [ ] `GET /api/sys/reports/{id}`：举报详情。
- [ ] `PUT /api/sys/reports/{id}/take`：接手举报，转为处理中。
- [ ] `PUT /api/sys/reports/{id}/handle`：处理举报。
- [ ] `PUT /api/sys/reports/{id}/reject`：驳回举报。
- [ ] `GET /api/sys/reports/{id}/logs`：处理日志。

### 3.5 状态流转

- [ ] `pending -> processing`：管理员接手。
- [ ] `pending -> processed`：管理员直接处理。
- [ ] `processing -> processed`：处理完成。
- [ ] `pending -> rejected`：直接驳回。
- [ ] `processing -> rejected`：核查后驳回。
- [ ] 终态举报普通管理员不可再次修改。
- [ ] 超级管理员可接管并终结异常举报。

### 3.6 测试

- [ ] 用户举报文章成功。
- [ ] 用户举报评论成功。
- [ ] 用户举报聊天消息成功。
- [ ] 举报不存在对象被拦截。
- [ ] 重复举报频率限制生效。
- [ ] 管理员接手后状态变为处理中。
- [ ] 终态举报重复处理被拦截。

## 4. 治理动作联动

### 4.1 删除内容

- [ ] 举报文章成立时可删除或下架文章。
- [ ] 举报评论成立时可删除或隐藏评论。
- [ ] 删除内容动作必须调用对应业务服务，不直接改表绕过一致性逻辑。
- [ ] 删除动作写入举报处理日志和系统审计日志。

### 4.2 撤回消息

- [ ] 举报聊天消息成立时可调用聊天后台撤回能力。
- [ ] 已撤回消息重复撤回保持幂等或给出明确业务错误。
- [ ] 撤回后需要推送消息更新事件。

### 4.3 禁言用户

- [ ] 支持按场景禁言：大厅、主题频道、群、全站。
- [ ] 记录禁言时长、原因、操作人。
- [ ] 禁言动作与聊天发送校验联动。
- [ ] 临时禁言到期后自动恢复或发送时动态判断。

### 4.4 封禁账号

- [ ] 支持账号禁用。
- [ ] 禁用后用户无法登录或无法继续使用互动能力。
- [ ] 封禁动作必须有高风险审计。
- [ ] 超级管理员可解封。

### 4.5 仅记录不处罚

- [ ] 支持举报成立但不处罚。
- [ ] 支持举报不成立并驳回。
- [ ] 两种情况都需要明确处理说明。

## 5. 审计增强

- [ ] 举报处理动作写入 `sys_report_handle_log`。
- [ ] 高风险动作同步写入系统操作日志。
- [ ] 审计记录包含处理前状态和处理后状态。
- [ ] 普通管理员不能删除审计记录。
- [ ] 超级管理员接管行为必须单独记录。

## 6. 通知预留

- [ ] 举报提交后后台可见。
- [ ] 举报处理结果是否通知举报人先预留字段。
- [ ] 被举报人是否通知先不强制实现。
- [ ] 后续如接入通知中心，应遵守用户通知设置。

## 7. 非目标

- [ ] 第一阶段不做申诉流程。
- [ ] 第一阶段不做自动审核。
- [ ] 第一阶段不做复杂风控画像。
- [ ] 第一阶段不做批量处理，除非后台需求进一步明确。

## 8. 完成标志

- 用户可举报文章、评论、聊天消息。
- 管理员可接手、处理、驳回举报。
- 处理动作能联动内容、聊天和账号治理。
- 举报状态、处理结果和处罚类型完整留档。
- 超级管理员接管和高风险动作具备审计记录。
- 对应 API 文档、需求文档和进度文档已同步更新。

