# 第二期任务清单导航

本文档是 `docs/tasks` 的当前入口。第一期任务清单已归档移除，后续研发从第二期重新规划，不再按一期 `01` 到 `09` 的执行文档推进。

## 1. 阶段基线

第二期默认以下能力已经作为后端基线存在：

- 账号认证、RBAC、作者申请、等级经验、超级管理员安全。
- 博客文章生命周期、审核、可见范围、系列文章、评论、点赞、收藏和附件引用。
- 私聊、群聊、大厅频道、主题频道、频道申请、群申请、邀请链接、频道与帖子轻量挂接。
- AI 基础问答、渠道配置、额度控制、调用统计和数据读取范围控制。
- 举报处理、会话级禁言、账号封禁、审计日志、通知偏好和后台首批数据看板。

第二期不再重复拆解上述一期能力，只在新增需求直接依赖时做局部补强。

## 2. 当前二期任务

第二期任务子项已完成并移除独立任务文档，当前目录仅保留本导航作为阶段状态入口。

后续新增任务时，应按新的阶段目标重新创建任务文档，并在本文件登记主题、状态、优先级和完成标准。

## 3. 已完成范围

- 论坛正式模块已完成公开侧、用户侧、后台治理、举报接入、通知联动、频道分享和测试补强。
- AI 知识库 / RAG / agents / 工具调用 / MCP 已完成主链路、数据范围校验、内容同步和基础扩展点。
- AI 多模态输入（图片附件）、SSE 流式输出和 Token 预算控制（input/history/rag/attachment/output 五维）已完成，含服务级测试。
- 外部博客迁移、用户自服务和友情链接已完成主流程、接口文档和服务级测试。
- 治理、通知和后台运营增强已完成禁言体系、举报通知、运营看板缓存和禁言范围测试。
- 性能、测试与代码质量补强已完成关注缓存、Redis Key/TTL 质量、核心 SQL 索引、权限测试和低风险代码质量收口。

## 4. 后续任务创建规则

- 新增任务必须先明确所属阶段、业务主题、验收标准和测试要求。
- 任务文档应只保留仍需推进的待办项，已完成子项及时回写状态。
- 阶段性任务全部完成后，可移除对应任务文档，并在本导航保留完成范围摘要。
- 前端可见接口变化必须同步更新 `docs/api文档`。
- 数据结构变更必须同步原始建表脚本和相关需求文档。
