# 文档导航

这份文档用于统一收口仓库内的主要说明入口，避免 README、API 文档、进度文档和专项说明分散后难以判断哪份是当前口径。

## 1. 项目级文档

优先看这些文档确认当前阶段状态与协作规则：

- `项目结构规范.md`：项目目录分层、模块组织、模型落位和新增代码放置规则。
- `项目代码编写规范.md`：代码分层、转换、异常处理、日志、缓存、测试、注释、接口文档与 Git 规则。
- `项目进度文档.md`：当前真实落地状态、风险与阶段判断。
- `后续更新计划.md`：后续阶段目标、优先级与模块推进方向。
- `db-schema-repair.md`：数据库存量库修复策略与脚本使用说明。

## 2. 前端联调文档

面向前端或接口联调时，优先从这里进入：

- `api文档/README.md`：接口文档导航页。
- `api文档/auth-api.md`：认证、RBAC、通知中心接口。
- `api文档/content-api.md`：文章、分类、标签、评论、收藏、互动、足迹接口。
- `api文档/file-api.md`：文件上传、我的文件、后台文件管理接口。
- `api文档/chat-api.md`：聊天用户接口、后台聊天管理与 WebSocket 协议说明。
- `api文档/follow-api.md`：关注、粉丝、互关状态、公开查看与后台治理接口。

## 3. 专项跟踪文档

- `tasks/content-domain/`：内容域历史拆分与任务跟踪资料。

## 4. 使用约定

- 涉及目录结构、模块边界、模型落位调整时，优先更新 `项目结构规范.md`，再修改代码或其他说明文档。
- 涉及项目阶段、已完成能力、优先级变化时，同时更新 `项目进度文档.md` 与 `后续更新计划.md`。
- 涉及前端可见接口变化时，同时更新对应 `api文档`。
- `README.md` 保留项目入口与运行说明；更细的阶段判断、规范和专项说明统一沉淀在 `docs/` 下。
- `chat` 已完成 v1 用户侧读写、单机 WebSocket 与后台管理接口；`follow` 已完成用户侧主链路、公开访客接口、后台治理接口与 Repository 收口。


