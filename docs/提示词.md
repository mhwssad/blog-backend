针对目录 E:\project\blog\blog-backend\src\main\java\com\cybzacg\blogbackend\module\auth 下的 Java 代码：

将所有可以使用声明式验证的字段（如非空、长度、格式、范围等）改为 JSR-303/Jakarta Bean Validation 注解（如 @NotNull、@Size、@Pattern 等）进行校验。

对于业务特有的校验逻辑（如唯一性、状态依赖等），请创建自定义验证注解（例如 @UniqueArticleTitle），并实现对应的 ConstraintValidator。

严格避免在 Service 层编写手动验证代码（如 if (xxx == null) throw ...），将验证职责完全交给注解驱动，保持服务层关注核心业务逻辑。

请扫描项目路径下的所有 Java 文件，将其中所有字符串处理操作（例如调用 `StringUtils.isBlank`、`StringUtils.isEmpty`、`Strings.nullToEmpty` 等，以及直接调用 `str.trim()`、`str.substring()` 等原生方法）统一替换为使用自定义工具类 `com.cybzacg.blogbackend.utils.StrUtils` 中对应的方法。若 `StrUtils` 当前缺少所需方法，请先在该类中补充相应静态方法，再进行调用替换。确保不再出现任何绕过 `StrUtils` 的字符串操作。

现在根据语义和功能优化下面的提示词， 需要消除语义歧义，明确功能边界，加强约束力度， 表达清晰、无多余修饰 以下是提示词:

提交操作
执行 Git 提交操作前，必须先执行项目编译命令（如 `mvn clean compile` 或 `gradle build`，根据项目实际构建工具确定），确保编译通过且无错误。仅在编译成功的前提下，才允许执行 `git add`、`git commit` 等提交操作。若编译失败，必须中止提交流程并报告错误。

任务：为指定工具包下的所有 Java 类补充文档注释和行内注释。
目标路径：`E:\project\blog\blog-backend\src\main\java\com\cybzacg\blogbackend\utils\` 目录下的所有 `.java` 源文件。
注释要求：

1. **文档注释（Javadoc）**：
   - 每个公共类或接口必须添加类级文档注释，描述该工具类的核心功能和用途。
   - 每个公共静态方法必须添加方法级文档注释，包含：
     - 功能描述
     - `@param`（参数说明）
     - `@return`（返回值说明，若 void 则省略）
     - `@throws`（若抛出检查异常或重要运行时异常）
   - 非公共方法（private/protected）建议添加简单文档注释。
2. **行内注释（单行 //）**：
   - 复杂逻辑块（超过5行的算法、循环、条件嵌套）前需添加行内注释解释意图。
   - 非常规业务处理（如 hack、workaround、性能优化）必须添加行内注释说明原因。
     约束规则：

- 不得修改任何方法签名、逻辑实现、异常抛出及字段可见性。
- 不得覆盖或删除已有的注释（仅在缺少注释处补充）。
- 对于空方法或纯粹 getter/setter（无业务逻辑），无需添加方法文档注释，但类注释必须保留。
- 补充完成后需保证编译无警告（不要求检查注释格式完美，但必须符合 Javadoc 基本语法）。
  输出要求：
- 输出每个被修改的文件名及补充注释的关键位置摘要。
- 若某些类或方法因缺乏上下文无法写出有意义的注释，需在输出中列出并请求人工补充说明。
  排除范围：
- 测试包（`/src/test/java/`）不在此任务范围内。
- 自动生成的代码（如 MapStruct 实现类、Lombok 生成的 toString 等）不要求补充。
