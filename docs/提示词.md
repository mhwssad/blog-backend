验证提示词：
针对目录 E:\project\blog\blog-backend\src\main\java\com\cybzacg\blogbackend\module\auth 下的 Java 代码：

将所有可以使用声明式验证的字段（如非空、长度、格式、范围等）改为 JSR-303/Jakarta Bean Validation 注解（如 @NotNull、@Size、@Pattern 等）进行校验。

对于业务特有的校验逻辑（如唯一性、状态依赖等），请创建自定义验证注解（例如 @UniqueArticleTitle），并实现对应的 ConstraintValidator。

严格避免在 Service 层编写手动验证代码（如 if (xxx == null) throw ...），将验证职责完全交给注解驱动，保持服务层关注核心业务逻辑。

