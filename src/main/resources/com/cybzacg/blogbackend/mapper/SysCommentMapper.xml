<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cybzacg.blogbackend.mapper.SysCommentMapper">

    <resultMap id="BaseResultMap" type="com.cybzacg.blogbackend.domain.SysComment">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="targetId" column="target_id" jdbcType="BIGINT"/>
        <result property="targetType" column="target_type" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="images" column="images" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="rootId" column="root_id" jdbcType="BIGINT"/>
        <result property="parentId" column="parent_id" jdbcType="BIGINT"/>
        <result property="likeCount" column="like_count" jdbcType="INTEGER"/>
        <result property="replyCount" column="reply_count" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="TINYINT"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
        <result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,target_id,target_type,
        content,images,user_id,
        root_id,parent_id,like_count,
        reply_count,status,created_at,
        updated_at
    </sql>

    <select id="selectRootCommentsByTarget" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_comment
        where target_id = #{targetId}
          and target_type = #{targetType}
          and root_id = 0
        order by created_at asc, id asc
    </select>

    <select id="selectRepliesByRootIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_comment
        where root_id in
        <foreach collection="rootIds" item="rootId" open="(" separator="," close=")">
            #{rootId}
        </foreach>
        order by created_at asc, id asc
    </select>
</mapper>
